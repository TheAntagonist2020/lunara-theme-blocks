# Reviews Archive Credibility Pass

contract: `/reviews/` and the Review CPT archive keep the existing Review archive renderer, URLs, sort links, pagination, spoiler markers, trailer badges, Oscar ledger pills, and Lunara typography.

contract: The support/companion lane becomes a theme-owned dynamic rail using `data-lunara-review-dynamic-rail` and `data-lunara-dynamic-rail`; no paid archive plugin assets are installed or enqueued.

invariant: Review card images keep the 3:4 card discipline and text-led cards remain intentional when media is missing.

invariant: Motion respects reduced-motion preferences and supports click, keyboard, focus/pointer pause, and touch navigation.

test: `tests/reviews-archive-credibility.ps1` verifies the renderer contract, asset enqueue, JS behavior markers, and no third-party plugin imports.

